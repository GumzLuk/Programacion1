program ProjectVStr;

uses
  Vcl.Forms,
  UFormVStr in 'UFormVStr.pas' {Form1},
  UClassVectStr in 'UClassVectStr.pas',
  UClasCadena2 in 'UClasCadena2.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
